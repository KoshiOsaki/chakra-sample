import { Box } from "@chakra-ui/react";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <Box bgColor="teal">
      <div>ああああ</div>;
    </Box>
  );
};

export default Home;
